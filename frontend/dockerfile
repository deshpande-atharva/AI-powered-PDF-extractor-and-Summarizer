# frontend/Dockerfile
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (not just production)
RUN npm install

# Copy all application files
COPY . .

# Expose port
EXPOSE 5173

# Run the dev server
CMD ["npm", "run", "dev"]